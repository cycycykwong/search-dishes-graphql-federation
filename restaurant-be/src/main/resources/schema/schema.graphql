type Query {
    restaurants: [Restaurant]
}

#type Dish @key(fields: "id") @extends {
#    id: ID!
#    restaurant: Restaurant
#}

type Restaurant {
    id: ID!
    name: String
    address: String
}